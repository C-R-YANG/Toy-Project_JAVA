<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Login">
    <insert id="insUser" parameterType="kr.co.ch.bori.user.login.dto.LoginDto" >
        INSERT INTO user
        (id, pw, email, use_yn, role)
        VALUES
        (#{id}, #{pw}, #{email}, true, 9)
    </insert>

    <select id="selId" parameterType="String" resultType="String">
        SELECT id FROM user WHERE email=#{email}
    </select>

    <select id="selPw" parameterType="kr.co.ch.bori.user.login.dto.LoginDto" resultType="String">
        SELECT pw FROM user WHERE id=#{id} AND email=#{email}
    </select>
</mapper>